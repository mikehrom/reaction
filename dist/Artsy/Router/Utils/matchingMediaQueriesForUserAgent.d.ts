import { MatchingMediaQueries } from "Utils/Responsive";
/**
 * Find the breakpoints and interactions that the server should render
 */
export declare function matchingMediaQueriesForUserAgent(userAgent: string): MatchingMediaQueries;
