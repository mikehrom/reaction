import React from "react";
export declare const NotificationsBadge: React.FC<{
    /**
     * If hovering over the nav item, `hover` is passed into the badge (Overlay)
     */
    hover?: boolean;
}>;
