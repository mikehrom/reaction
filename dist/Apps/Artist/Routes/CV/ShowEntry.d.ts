import { SerifSize } from "@artsy/palette";
import { SFC } from "react";
interface ShowEntryProps {
    node: any;
    size?: SerifSize;
}
export declare const ShowEntry: SFC<ShowEntryProps>;
export {};
